# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# claude
.claude

# uv source (cloned on demand by scripts/apply-patches.sh)
/uv

# Added by cargo
/target

# Nix build output
/result

# Test cache (persistent Python installs across runs)
/.cache

# Rust
/.cargo

# cached-exec
/.cached-exec
